import { User } from './users.model';

export class Task {
    // taskid: string;
    name: string;
    createdDate: string;
    assignedBy: string;
    assignedByName: string;
    assignedByPhotoUrl: string;
    done: Boolean;
}
